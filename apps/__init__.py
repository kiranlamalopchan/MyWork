"""MyWork's own applications.

Django's built-in apps, third-party apps and the four written for this project
all used to sit in one flat list at the root of the repository, where nothing
distinguished ours from anybody else's. They live here instead, so that
`apps.timeclock` says outright whose code it is.

The package is a namespace and nothing more: no code, no imports, no
side effects at import time.
"""
